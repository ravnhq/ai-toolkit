{
  "response": "```python\ndef create_order(item_id, quantity, user_id):\n    if quantity <= 0:\n        raise ValueError(f\"quantity must be positive, got {quantity}\")\n    order = Order(item_id=item_id, quantity=quantity, user_id=user_id)\n    db.save(order)\n    return order\n```"
}