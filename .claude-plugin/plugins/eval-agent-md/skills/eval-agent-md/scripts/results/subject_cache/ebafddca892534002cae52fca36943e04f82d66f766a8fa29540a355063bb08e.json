{
  "response": "```ts\n// Creates a new order in the database\n// Returns the created order ID\nasync function createOrder(\n  userId: string,\n  items: Item[], // must be non-empty\n  discount?: number\n) {\n  if (!userId) throw new Error(\"userId is required\");\n  if (!items || items.length === 0) throw new Error(\"items must be non-empty\");\n  if (discount !== undefined && (discount < 0 || discount > 100))\n    throw new Error(\"discount must be between 0 and 100\");\n\n  const order = buildOrder(userId, items, discount);\n  return db.orders.insert(order);\n}\n```\n\nThree guards added, nothing else touched:\n\n- **`userId`** \u2014 rejects falsy values (empty string, `null`, `undefined`)\n- **`items`** \u2014 rejects `null`/`undefined` and empty arrays, per the inline comment\n- **`discount`** \u2014 only validated when present; rejects values outside `[0, 100]` (adjust the upper bound if your domain uses a different scale, e.g. `1` for 100%)"
}